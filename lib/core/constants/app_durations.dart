class AppDurations {
  static const ultraFast = Duration(milliseconds: 110);
  static const fast = Duration(milliseconds: 120);
  static const normal = Duration(milliseconds: 200);
  static const page = Duration(milliseconds: 260);
  static const panel = Duration(milliseconds: 400);
  static const transition = Duration(milliseconds: 600);
  static const boot = Duration(milliseconds: 3200);
  static const backgroundLoop = Duration(seconds: 20);
}
